/// @description Insert description here
// You can write your code in this editor
//Gui
if room = setup_Room
{
	draw_text(100, 100, "Press enter on keyboard or start on controller")
	draw_text(600, 100, "Press enter on keyboard or start on controller")
	draw_text(100, 600, "Press enter on keyboard or start on controller")
	draw_text(600, 600, "Press enter on keyboard or start on controller")

	draw_text(100, 200, playerInputDevice[0])
	draw_text(600, 200, playerInputDevice[1])
	draw_text(100, 700, playerInputDevice[2])
	draw_text(600, 700, playerInputDevice[3])
}
